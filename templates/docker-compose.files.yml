services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    container_name: serverfiles-filebrowser
    restart: unless-stopped
    ports:
      - "8082:80"
    volumes:
      - ./data/files:/srv
      - ./data/filebrowser/database:/database
      - ./data/filebrowser/config:/config
